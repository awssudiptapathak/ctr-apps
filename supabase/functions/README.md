# Supabase Edge Functions

This folder is reserved for server-side business logic that must not be exposed to the mobile or browser clients.

Planned Phase 1 placeholders:
- `send-notification`
- `process-reminders`
- `allocate-slots`
- `send-whatsapp`

All privileged flows should live here or in PostgreSQL functions/RPCs.
